This is a program that will notify a user when somebody gets on the HC Factions server.  A person can add "watch" players to the list.  A time interval to check (refresh) can be set.  Coded by MattyHawk (IGN on HC Factions).